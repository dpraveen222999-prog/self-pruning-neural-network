# self-pruning-neural-network
PyTorch implementation of a self-pruning neural network using learnable sigmoid gates. Trains on CIFAR-10 with L1 sparsity regularization to induce structured pruning during training. Evaluates accuracy–sparsity trade-offs across multiple λ values with visualizations and analysis.
